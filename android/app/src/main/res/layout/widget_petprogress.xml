<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:orientation="horizontal" android:padding="8dp"
  android:background="@android:color/transparent"
  android:layout_width="match_parent" android:layout_height="wrap_content">

  <ImageView
    android:id="@+id/pet_image"
    android:layout_width="48dp" android:layout_height="48dp"
    android:src="@mipmap/ic_launcher" android:contentDescription="@string/app_name"/>

  <LinearLayout
    android:orientation="vertical" android:layout_width="0dp"
    android:layout_height="wrap_content" android:layout_weight="1"
    android:paddingStart="8dp">

    <TextView
      android:id="@+id/txt_task"
      android:text="Your Hourly Task"
      android:textSize="12sp" android:maxLines="2"
      android:layout_width="match_parent" android:layout_height="wrap_content"/>

    <LinearLayout
      android:orientation="horizontal" android:layout_width="wrap_content"
      android:layout_height="wrap_content" android:paddingTop="4dp">

      <TextView android:id="@+id/btn_prev"     android:text="‹" android:textSize="14sp" android:padding="6dp"/>
      <TextView android:id="@+id/btn_complete" android:text="✓" android:textSize="14sp" android:padding="6dp"/>
      <TextView android:id="@+id/btn_skip"     android:text="✕" android:textSize="14sp" android:padding="6dp"/>
      <TextView android:id="@+id/btn_next"     android:text="›" android:textSize="14sp" android:padding="6dp"/>
    </LinearLayout>
  </LinearLayout>
</LinearLayout>
